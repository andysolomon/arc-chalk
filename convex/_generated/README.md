# Generated Convex files

These files stand in for `npx convex codegen` so the workspace typechecks without a Convex login. Running `npx convex dev` (development only — never `npx convex deploy` during development) regenerates them against a deployment.
