import { defineSchema } from "convex/server";

// Tables are introduced with their domain contracts in Phase 7. Keeping the
// schema empty now makes the backend boundary executable without inventing data.
export default defineSchema({});
