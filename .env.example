# Public client identifiers only. Never commit credentials or private keys.
VITE_CONVEX_URL=
VITE_CLERK_PUBLISHABLE_KEY=

# Server-only values are configured in the relevant deployment environment.
CLERK_JWT_ISSUER_DOMAIN=
R2_ACCOUNT_ID=
R2_BUCKET_NAME=
