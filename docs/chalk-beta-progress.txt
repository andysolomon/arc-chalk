chalk-beta - Chalk Production Beta - Progress
Generated: 2026-08-04 16:50:45 EDT
Updated: 2026-08-22 11:30:00 EDT
On completion, move this file and the plan to docs/archive/.

[ ] 0.0 - Phase 0 - Canonical prototype inventory and parity lock
    [x] 0.1 - Establish the restored original as the canonical visual and behavioral specification
    [x] 0.2 - Create the governing parity matrix and mark the alternate design exploration rejected
    [ ] 0.3 - Capture every top-level view, panel, menu, modal, selection state, and supported viewport
        Evidence: the canonical Playwright harness now locks Editor, Demo, Present, Print, and seven global editor overlays at the first desktop viewport; remaining states and viewports are still open.
    [ ] 0.4 - Inventory every control, action, shortcut, gesture, persistence transition, sample, and output at item level
    [ ] 0.5 - Create golden documents, generated outputs, and behavior scripts for every parity group
    [ ] 0.6 - Review and record any explicit parity exceptions before major implementation
[x] 1.0 - Phase 1 - Workspace and architectural guardrails
    [x] 1.1 - Scaffold the Bun workspace, Vite React TypeScript web shell, and root Convex directory
    [x] 1.2 - Create domain, contracts, editor, render, local-db, sync, exports, and test-fixtures packages
    [x] 1.3 - Add shared typecheck, lint, format, unit, browser, and build commands
    [x] 1.4 - Enforce inward dependency rules and document local setup/environment contracts
[x] 2.0 - Phase 2 - Domain, geometry, and deterministic rendering core
    [x] 2.1 - Implement Zod Mini domain schemas, stable IDs, envelopes, migrations, canonical hashing, and serialization
        Evidence: schema-version-3 Plays plus versioned Playbooks, Concepts, Formations, Field Profiles, hybrid Assignments, immutable revisions, Play and Playbook envelopes, and sanitized Share Publications validate structural and referential invariants. Unit is the only required classification; built-in and Coach-defined Play Types are stable definitions with optional Play snapshots. Pure v1-to-v2-to-v3 migrations and permanent released-version fixtures preserve legacy Field Profiles and promote route assignment prose without loss.
    [x] 2.2 - Implement Field Profiles, yard geometry, landmarks, snapping, and exact mirroring
        Evidence: versioned NFHS, NCAA, NFL, and custom profiles use explicit yard units and derive bounded landmarks; exact double mirroring preserves canonical geometry; and the pure editor seam deterministically ranks ball/LOS, hashes, sidelines, yard marks, nearby player/route-node alignment, equal splits, and configurable grids at a zoom-independent screen threshold. It returns transient visible guides and group-preserving deltas, supports toggle/temporary-disable inputs, and suggests or constrains grass-true 45-degree route breaks. Corrected on 2026-08-08: the geometry seam was right, but the conversion feeding it was not. The original's canvas is anisotropic — 18.3 pixels per yard across the field against 12 downfield — and the legacy reader used the depth scale on both axes, so every crossfield distance it produced was half again too large. That is review finding #3, reproduced in the conversion rather than inherited from it, and it put the seeded Play's split end five yards out of bounds. A second defect hid the first: field markings were drawn on a lateral scale the Players did not share, stretching the sidelines 177 pixels wider than the Play, so an out-of-bounds receiver landed just inside a line that was itself in the wrong place. Both axes now convert on their own scale, one projection draws Players, paths, labels and markings alike, and a Player standing on the sideline is drawn on the sideline for every built-in profile. Tests pin the conversion to the original's own 976-pixel field span, assert the drawn field fills its frame rather than only agreeing with itself, and hold every seed and golden inbounds; each was confirmed to fail against the defect it guards.
    [x] 2.3 - Implement structured paths and absolute integer-millisecond evaluation
        Evidence: every original path kind, line style, ending, curve, per-segment override, choice branch, alternate, tick, and coverage area now projects from yard-space data; bounded quadratic sampling drives deterministic integer-millisecond evaluation.
    [x] 2.4 - Build framework-free RenderScene and accessible SVG primitives
        Evidence: the framework-free render seam now projects Field Profile markings; every original path stroke, branch, tick, coverage area, and ending; all six player symbols and three fills; and deterministic role-aware labels, boxes, bound positions, and solid/dashed leaders. React paints prepared SVG geometry and exposes accessible Play, Player, and Label names without reinterpreting domain styling.
    [x] 2.5 - Add golden fixtures, round-trip tests, property tests, and deterministic-render tests
        Evidence: realistic 11-player offensive and defensive Playbook envelopes cover custom Play Types, Concepts, Formations, stable source revisions, slot bindings, hybrid Assignments, routes, coverage, and pressure. Permanent v1/v2 migration fixtures plus generated JSON round trips, canonical key-order/SHA-256 properties, schema-upgrade properties, dangling-reference properties, malformed-document cases, exact double mirrors, deterministic path evaluation, and Coach-visible render tests pass without storage or network dependencies.
[x] 3.0 - Phase 3 - Local database, durability, and recovery
    [x] 3.1 - Implement typed Dexie repositories and all authoritative and derived stores
        Evidence: the fixed version-1 Dexie schema is private behind the ChalkLocalRepository interface and separates Playbooks, Concepts, Formations, current Plays, immutable revisions, durable sync mutations, conflicts, preferences, image blobs, per-Play undo history, search projections, and thumbnail derivatives. The local-db boundary rejects React, Convex, TanStack, editor, render, sync, and export imports. Nine real IndexedDB behavior tests cover both realistic Playbook goldens, every store family, durable close/reopen, disposable derived records, runtime domain validation, no partial records from invalid aggregates, and beta-scale commit performance.
    [x] 3.2 - Implement transactional local commits and continuous save-state acknowledgement
        Evidence: the Chalk-owned vanilla Zustand EditorStore keeps title typing transient until blur or Enter, validates committed Play documents, serializes rapid saves, advances optimistic document-hash guards, creates durable sync mutations, and reports Saving on this device, Saved on this device, or Local save failed — retry without leaking React or Dexie through its interface. The measured interval includes queue wait plus IndexedDB acknowledgement. Unit tests cover draft/commit separation, ordered rapid edits, failure retention, retry, and budget classification; committing one Play into a 2,000-Play Playbook costs no more than committing into a two-Play Playbook, proving the commit path does not grow with Playbook size; and Chromium plus WebKit/iPad tests prove the Coach-visible acknowledgement, truthful budget classification, and close/reload durability through the production adapter. The strict 50 ms budget is asserted on developer machines and is guarded against gross regressions on shared CI runners, which are not target devices; item 4.6 owns proving it on the target devices.
    [x] 3.3 - Implement bounded persistent hash-guarded per-Play undo and redo
        Evidence: a Coach edit is now a semantic Play command — rename, notes, tags, Personnel Label, Play Type, unit, Concept and Formation source, Field Profile, Player insert/remove/move/update, route insert/remove/update, label and Assignment insert/remove/update, mirror, and one batch per gesture — carrying an inverse computed against the Play as it stood before the edit and producing a schema-validated Play. Deleting Players or routes cascades to dependent routes, bound labels, dangling Assignment actions, and Formation slot bindings as one undoable step, and clearing a layer is an ordinary hash-bound transaction. Per-Play history stores forward and inverse commands with expected before and after document hashes instead of Play snapshots, coalesces consecutive edits to one field until the Coach pauses, blurs, or moves to another field, clears only that Play's redo stack on a new edit, and retains at most 100 commands, 20 MiB of encoded history, or seven days. A step is offered only when the Play in front of the Coach hashes to the entry's expected boundary; anything stale, unreplayable, or hash-mismatched is quarantined instead of applied speculatively. History rides inside the Play commit transaction, stays device-local, and is discarded rather than trusted when it no longer parses. Command round trips, generated command-sequence properties, retention bounds, coalescing, redo clearing, quarantine, and restore-after-reopen pass without React or IndexedDB; real IndexedDB tests cover the transactional history write, stale-commit rollback, corrupt-history disposal, and a full 100-entry history committed at 2,000-Play scale without growing with Playbook size; and Chromium plus WebKit/iPad prove Coach-visible Undo and Redo through the production adapter across reloads.
    [x] 3.4 - Implement named versions, Trash retention, startup recovery, quota handling, and persistent-storage UX
        Evidence: a Coach marks the Play as it stands with a chosen label, and revision IDs are added rather than put so no automatic process can rename or overwrite a named version; a Play's history lists as metadata only. Restoring diffs the current Play against the version and applies the difference as ordinary commands, so a restore is one undoable entry rather than an out-of-band replacement, satisfying both the no-snapshots rule and the original's undoable restore. Deleting a Play hides it from the Playbook and drops its derived data while keeping the document, its versions, and its history recoverable for thirty days; a Play in the Trash cannot be committed to, and purging after retention is the only path that discards a Coach's Play. An open-session marker records that a session started and is cleared on a clean close, so the next startup can say the app closed unexpectedly without claiming work was lost. Chalk requests persistent storage, never asking twice once granted, grades quota pressure as healthy, watch, critical, or unknown, reports unknown rather than failing when the storage API is blocked, and offers to free the disposable previews and projections it can rebuild. The shell replaces the ambiguous Save button with an explicit Create version action and a save acknowledgement that is pressable only to retry a failure; Sync now stays deferred to Phase 7. Command-difference round trips and a generated property that any edit sequence reproduces its target, real IndexedDB tests for versions, Trash, purge, interrupted sessions, and storage grading, component tests for the version menu, recovery notice, storage pressure, and retry affordance, and Chromium plus WebKit/iPad proof that a named version outlives a reload and restores undoably all pass.
    [x] 3.5 - Add real IndexedDB migration fixtures and interrupted-write recovery tests
        Evidence: a fixture harness writes released records straight through IndexedDB rather than through the current repository, so the suite upgrades a database a previous release actually wrote instead of one today's code produced. That harness exposed a real durability defect: a stored Play at schema version 1 or 2 made both getPlay and loadPlaybook throw, so a Coach whose database predated the current schema could not open their Playbook at all. Reads now migrate a legacy Play forward, keep it in its own Playbook rather than the legacy-import bucket, and rehash it because the stored hash described the older shape; an explicit startup upgrade rewrites those Plays once, skips any a concurrent tab already moved, restores their search projections, and is a no-op on second run. A current Play whose hash no longer matches is still reported as corrupt rather than silently migrated. Interrupted-write coverage proves the Coach is left with either the prior valid state or the complete new state and never a mix: closing the device mid-commit rolls the Play, its revision, its sync mutation, and its undo history back together; a transaction that fails after writing leaves no history describing the rejected edit; losing every disposable derived record costs no Play; and a commit that was acknowledged before the tab died survives with its history and mutation intact.
    [x] 3.6 - Implement and verify encrypted backup export/import
        Evidence: a Coach's backup is encrypted on the device before it becomes a file, using PBKDF2-SHA-256 at 600,000 iterations over a passphrase the Coach chose and AES-GCM with the header bound as additional authenticated data, so altering the recorded work factor or the ciphertext makes the file refuse to open. A wrong passphrase and a tampered file are reported identically, because both mean the same thing to the Coach. Every export uses a fresh salt and nonce, and no Play name, Playbook name, or Assignment text appears anywhere in the file. The payload carries the Coach's authoritative work and its immutable history — Playbooks, Concepts, Formations, Plays including their Trash marks, revisions, and preferences — while device-local queues, conflicts, undo history, the open-session marker, and derived previews stay behind. Backups are written strictly and read leniently: a file an earlier release wrote has its Plays upgraded on import and restored to the Playbook the Coach kept them in. Import validates everything before writing anything and writes in one transaction, so a rejected file leaves the device exactly as it was; merging never overwrites a Play the Coach edited after the backup was written and never rewrites an immutable version already stored, and replacing is the only way to discard current records. Round trips restore current work, Trash state, and named versions onto a new device with matching document hashes; the shell offers backup and restore with the passphrase the Coach chose, warns plainly that a lost passphrase cannot be recovered, refuses to send anything without one, and never uses replace mode.
[x] 4.0 - Phase 4 - Production editor and responsive interaction model
    [x] 4.1 - Faithfully recreate the original visual shell and states using production UI primitives underneath
        Evidence (2026-08-18 shell slice): the rail now uses the canonical 18-unit prototype glyphs; angle snapping is an accessible mouse/keyboard toggle with live SNAP state; and the status bar now exposes live zoom, fit, selection, ball-centering, formation, and count controls. Chromium and WebKit/iPad shell tests plus the full check pass against the existing parity ratchets; the remaining shell work and target-device performance proof stay open.
        Evidence (2026-08-21 books slice): both books are now the Coach's own. A set or a call carries a star that keeps it within reach, favorites sort to the top of whatever is showing, and the All / Favorites / Mine tab strip rides in the head beside the search exactly where the original puts it — so nothing below it moves. The offense standing on the field can be kept as a set of its own from the footer: the domain reads the whole set off the men — personnel, strength, a role for every man so a route survives being realigned into it, the ball they are aligned to and the hash it sits on — and only the name is the Coach's to give. Defenders are left out, a name he has used already replaces the set that had it, and naming a set stars it at once, as the original does, because he named it to reach for it. Saved sets and stars live on the device — sets in the Formations table, stars in preferences under the original's own keys — so they never enter a Play's document and never travel with one; the set on the field is recognized from both books at once, and so is the ghost the browser draws. Five unit tests and one Chromium/WebKit e2e prove a star, a save, a removal and both books' favorites survive a reload against real IndexedDB, waiting on the stored record rather than on a timer; two reintroduced defects proved the domain tests non-vacuous and two more the component tests. The parity ratchets fell to 1.98% for the Formations browser and 1.57% for the Defenses browser. What still holds Formations above the Editor's own gap is its cards, not its chrome — row drift down the grid and the thumbnail dot arithmetic, both of which predate this slice. A set the Coach saved reaches the palette when `paletteCommands` stops being a module constant, which is where the saved-Play entries arrive too.
        Evidence (2026-08-21 Present / Print / Page / Type slice): Present is a full-window field on #171717 with the play name, `← → variations`, and `esc` back. Type scales 1.25× on top of whichever preset is selected, rounding after the scale the way the original's TYPE table does. Print is the letter-landscape sheet with title, category, field, "Print this", and `letter landscape · half-inch margins · {type} type`. Export → Print the field is that same print: a clone of the field reset to the full frame, with selection, handles, and the draw-a-route dot stripped, and the field-marking strokes carried as a stylesheet so yard lines survive a document that has no app CSS. Page kinds — Full field, Half field, Scout card, Playbook page, Blank — change what prints under the play without moving the players; Half field clips to the original's [196, 620] band, Scout card is the LOS alone, Playbook page is light lines, Blank is nothing. Coach / Player / Print type presets scale the words, Player hides conversions and coaching notes while leaving the Notes toggle on, and Print paints every fill in ink. Reads, Assignments, Notes, and Text each drop off independently. History ago-stamps match the original's buckets. The animation scrubber and the `1 / 5 · STICK — THUNDER` variation line wait on later phases. Parity first-measured Present at 17.96% (the field-aspect gap now filling the window) and Print at 0.84%. Unit tests cover the presentation seam, the print sheet, and the inspector controls; Chromium and WebKit/iPad prove Present, Print, page kinds, and the Text layer.
        Evidence (2026-08-21 Demo/Tour + B1): Demo is the original's five guided sequences — Tool tour, Quick routes, Blocking, 7-on-7 · no line, Defense — with the original's step copy, inspector teaching rows, ghost cursor, and Back / Pause-Play-Replay / Next. Opening a tour in the editor is a new Play with no prior identity: EditorStore.adoptPlay writes a new record and leaves the Play that was open byte-for-byte unchanged (parity-matrix B1). Tools is Stick — Thunder; Defense is Cover 3 — Fire Zone. Parity first-measured Demo at 2.12% (29,338 px). Unit tests cover the catalogue, playback, and the identity reset; Chromium and WebKit/iPad prove the tour chrome and the handoff against real IndexedDB.
        Evidence (2026-08-21 field viewBox): the field is drawn in the original's own 1000×620 frame at 12 pixels per yard of depth, with the 53⅓-yard width spanning the 976-pixel inset the original used. The editor column no longer shrinks that picture into a 1068×525 card. Yard space is still the source of truth; only the projection frame changed. Editor 1.65% (22,846 px), Present 8.99% (124,322 px), Print 0.80% (11,117 px), Demo 2.05% (28,317 px). Formations and Defenses ticked up 317 px because more of the Play shows around those panels; that raise is the field-aspect slice, not a chrome regression. Remaining 4.1 shell work is tool-rail glyphs below the text tool, status-bar spacing, Formation-card row drift, dynamic paletteCommands, and the minimap.
        Evidence (2026-08-21 rail glyphs): Clear, Snap, and Hide-the-tools now stamp the original's own `toolIcon` — width/height 18, stroke on each mark, no wrapping group — and the rail Clear button is isolated from header-menu padding. Live `getBoundingClientRect` against the original puts every rail SVG at 18×18 at x 19, Clear/Snap at 40×40 (y 808 / 852), and collapse at 40×22 (y 896, line-height 12 px). The Editor ratchet stayed 1.65% (22,846 px): those pixels were already the original's, and the remaining Editor gap is status-bar spacing plus Finding #3's Player shift. Remaining 4.1 shell work is status-bar spacing, Formation-card row drift, dynamic paletteCommands, and the minimap.
        Evidence (2026-08-22 visual shell leftovers): the status bar is the original's 30 px row — `line-height: normal`, no `space-between`, hint on the left, zoom cluster at gap 12 — and the hint is the original's own, so at-fit Select copy gives way to the grass-drag line once the view moves, and a route in progress or another tool speaks for itself. Formation cards put the star and the remove in the name row the original does, so later rows no longer drift, and thumbnail dots snap to the original's tenth of a pixel before the layout math so a converted line of scrimmage stays one row. `paletteCommands` is a function of the books the Coach has: a set he saved and a Play he named reach the catalogue, Hide/Show zone areas follows the live state, and applying a saved set from the palette is the same pick the browser runs. The navigator is the original's 132×82 map, shown once the camera is inside 0.985 of fit, with the LOS, the men, and a press that centres the view. The catalogue now concatenates the original's own way — Keyboard shortcuts, then exports, then every Formation, then every Defense, then Open: plays — so searching "form" reaches a set rather than a browser opener the original never listed; ⇧⌘F / ⇧⌘D still open the books. Label-hidden copy uses the original's `colW / cam.w` scale rather than a rounded zoom percentage. Remaining Editor gap is Finding #3's Player shift against goldens captured from the uncorrected original. Phase 4.1 is complete.
    [x] 4.2 - Implement EditorStore transient/committed state separation and unified input state machine
        Evidence: one pure yard-space state machine in the editor package now turns mouse, touch, and Pencil pointer input plus keyboard operations into the same selection changes and semantic Play commands, so no modality can disagree about what a gesture means. The shell converts client pixels through the SVG projection before dispatching, captures the pointer so a gesture survives leaving the field, and holds every mid-drag change as a transient preview built by the same command builder the release commits — exactly one hash-guarded transaction and one undo entry per completed gesture, with nothing written while the pointer moves. The machine reproduces the original's gesture grammar measured from the canonical source: a press becomes a drag only past two pixels, Shift settles membership on the press, a click on a multi-selection narrows to the clicked item on release, empty-grass presses become the marquee only past three pixels, and the marquee counts Players by center, routes by any point, and labels by their drawn anchor. A lone Player snaps landmark-first with the original's guides and depth readout while a group moves raw and keeps its shape; routes travel with the man running them, branch points and bound-label offsets included. Deleting a mixed selection composes the domain's dependent cleanup into one batch, Escape steps outward from gesture to selection exactly as the original did, a cancelled pointer reverts cleanly, and arrows nudge as the keyboard alternative ADR 0016 requires, with touch targets widened to the 44 CSS px minimum. Selection halos, the marquee, snap guides, and the readout render in the original's selection blue only while they apply, so the parity ratchets did not move. Twenty-eight machine tests were proven non-vacuous by reintroducing four defects, and Chromium plus WebKit/iPad drive real drags: a Player and his route move as one undoable step, a marquee delete undoes as one entry, and an abandoned drag leaves no trace. Drawing tools and node handles remain 4.3; camera pan, zoom, and pinch remain with the camera work in 4.4.
    [x] 4.3 - Implement every original player, route, branch, label, assignment, styling, snapping, and direct-manipulation behavior
        In progress — route creation landed on 2026-08-09. A Coach draws from the blue dot above a Player or with the Route, Motion, Block, and Zone tools: press to start on the man running it, press each break, and finish on Enter or a double click, committing one insert and one undo entry for the whole route rather than one per break. The drawing grammar is the original's, measured from the canonical source: breaks constrain to grass-true 45-degree increments while snapping is on and Shift frees them, a break landing within four pixels of the last is dropped, holding the pointer and pulling away past seven pixels bends that segment through the pointer by reflecting the control point across the chord's midpoint, and typed digits set a break's exact depth with Backspace trimming the number before it touches the route. Escape and Backspace step outward through the drawing — the typed depth, then the last break, then the route — before either reaches the Play, and switching tools abandons the route in progress. Routes clamp to the sidelines and the drawn frame, with rounding applied before clamping so a rounded value cannot cross back over the boundary it was just held inside. Each kind carries the original's defaults, and a second route on the same man arrives dotted as his alternate. Zone drops now classify by where they end and draw the original's default bubble until the Coach sizes them, with the classification thresholds converted on the correct axis. Thirteen machine tests and three render tests were proven non-vacuous by reintroducing seven defects; Chromium and WebKit/iPad draw a real route through the tool and through the blue dot, prove Escape leaves nothing behind, and prove the finished route undoes as one entry. Route editing followed on 2026-08-09: a selected route raises a handle on every break, a square in the middle of every segment, and the zone corner, all drawn at a constant screen size with hit areas that clear the 44 CSS pixel touch minimum. Dragging a break constrains it to 45 degrees from the break before it and then lets football landmarks claim it — the original's order, so an angle the Coach set is not undone by a landmark — and a curved segment keeps its bend because the control point travels with the break it belongs to. The curve handle rides the middle of its segment, bends it through the pointer, and straightens it again when dropped back within five pixels of the chord's midpoint, which is the only way back to a straight line once one is bent; the key is dropped rather than cleared so a straightened segment hashes identically to one never bent. The zone corner sizes the area a defender owns within the original's bounds, converted on the axis each belongs to, and reads out its width. Double-clicking a selected route adds a break to the segment the Coach pointed at. Each completed handle drag is one update and one undo entry, and a handle pressed without dragging only selects its break. Twelve more machine tests, mutation-checked against five reintroduced defects — one of which exposed a vacuous insert test that only ever exercised the first segment — plus Chromium and WebKit/iPad proof that a break drags and a segment bends through the real handles. Labels and the text tool followed on 2026-08-09, so a Coach can now finish a Play diagram without leaving production: the text tool writes a note where he pressed, hands the select tool back, and puts him straight into typing it. A note belongs to whichever unit he was working on rather than to where on the field it sits, because depth notes and progression numbers live downfield too. The Text panel replaces the idle inspector only while one note is selected — the state every parity golden captures is untouched, so the ratchets did not move — and offers the original's text field, Belongs to, Meaning, Size, Box, and colour swatches, where picking a meaning sets the whole look so a Landmark reads like every other Landmark on every card. The six meanings and their appearances now live in the domain beside the zone classification, being football semantics rather than shell styling. Retyping coalesces into one undo entry until the Coach leaves the field, and the input renders from a transient draft because binding it straight to the asynchronously committed text dropped keystrokes that landed mid-save. A note's leader line is a fourth handle kind, dragged to point at whatever the note is about. Two defects surfaced while wiring it, both invisible to unit tests and caught by driving the real browser. Selection was pruned of anything whose commit had not yet landed, so a Player, route, or note was deselected in the instant between the Coach making it and the save arriving; pruning now holds pending creations. And the field's default mousedown moved focus to the document body, tearing it back out of a note the Coach had just been put into typing; the field prevents that default, since it is driven by pointer events and needs none of it. Copy, paste, duplicate, and mirroring closed out the direct-manipulation verbs on 2026-08-09. The clipboard is transient editor state holding whole entities, so a paste survives edits to what it came from, or its deletion. Pasting makes new Players, routes, and notes rather than second references: a route follows its Player when both were copied, a note bound to a copied route rebinds to the copy, and one bound to a route left behind keeps its words and is placed by hand with the binding key dropped so it hashes like a note that was never bound. Copies land clear of their originals by the original's twenty-six canvas pixels, converted on the axis each belongs to. Duplicate is a copy and a paste in one press that neither reads nor disturbs whatever is on the clipboard. Mirroring with nothing picked is the domain's own reflection, which the double-mirror identity property already guards; mirroring a selection reflects only what the Coach picked, carrying each man's routes with him the way they follow when he is dragged, reflecting leader endpoints, and reflecting a bound note's offset rather than its position because the note rides its route. The Mirror action the More menu and command palette already listed is now real rather than shown unavailable. One divergence is recorded where it is made: the original detaches a route pasted without the man running it, which the production schema — requiring a Player on every path — has no way to express, so the copy stays attached to him instead. Eleven machine tests mutation-checked against five reintroduced defects, and Chromium and WebKit/iPad prove a paste is one undoable entry and that mirroring twice returns the Play exactly as it was. Branch and segment selection landed on 2026-08-09, together with a split of the interaction module into seven along its own seams — model, geometry, commands, clipboard, handles, drawing, and the machine that sequences them — done at this point because this is the slice that introduces a second axis of selection state rather than another verb. The split was pure movement and the two hundred and fifty-five tests then passing did so untouched, which is the only claim a refactor of that size can make for itself. A route is one selectable thing but has more than one line, so hit testing now reports which line was meant. The first click takes the route entire; a second, deliberate click narrows to the branch the Coach pointed at or to the segment of the main line nearest his press, exactly as the original let him reach one piece without a second control. Whether it was already the only thing picked is captured when the press begins rather than read back afterwards, since by release the press has already selected it. Handles then read and write whichever line is selected, and a branch's first break measures from the point on the main line it was split off at rather than from nothing, so its 45-degree constraint and its bend behave like any other break's. Picking something else forgets the line and the break, because they belonged to what was just left. Unselected branches show where they split off, and the selected line or segment is washed in the original's selection blue. Wiring it exposed a defect of our own making: handle targets had been widened to the forty-four pixel touch minimum for every pointer, and at that size a curve handle covers the middle of a short segment, so the segment underneath could not be clicked at all and segment selection was unreachable with a mouse. Targets are now sized by pointer modality — the touch minimum on a coarse pointer, the original's smaller targets and its precision on a fine one. Eight machine tests mutation-checked against five reintroduced defects, one of which exposed a branch test that could not distinguish reading the main line and writing it into the branch, and Chromium and WebKit/iPad prove the narrowing grammar including that a deliberate second click narrows where a quick one inserts a break. Route styling followed on 2026-08-09 and is where the previous slice pays off: selecting a route opens the original's Route panel in place of the idle one — kind, line, ending, colour, Straighten and Delete — and what it changes follows what the Coach has picked out. A segment takes a line style on its own, the way the original let him dot one leg of an otherwise solid route; a branch takes it for that line; otherwise the whole route does, and the piecemeal overrides go with it so the line reads as one thing again. Colour has no per-segment form in the contract and had none in the original either, so it is always the whole line's. Changing what a line is changes how it reads: each kind carries its own look, some of it fixed — a blitz is red, a stunt runs on chevrons — and some deferring to what he already chose, so turning a block back into a route keeps his ending unless that ending only made sense as a block. Those kind looks now live in the domain beside the label meanings and the zone classification, and the drawing tools draw from the same source rather than a second copy. Straighten takes the bends out of the line he is working on and leaves the others alone. Two defects surfaced. Pruning rebuilt the interaction model on every document change and silently dropped the line and break the Coach had narrowed to, so restyling the very segment he had picked out un-picked it and he could not make two changes to one piece in a row. And the line-style buttons carried their glyph as their only name, so a screen reader announced a dashed line as two en dashes; the glyph is now marked decorative and the button carries the word. Ten machine tests mutation-checked against five reintroduced defects — one of which showed the pruning test proved nothing until something was actually pruned, since with nothing to drop the function returns early and never reaches the rebuild — and Chromium and WebKit/iPad restyle a whole route and then one leg of it. Clear-by-layer landed on 2026-08-09 and turned out not to be a layer at all: the original's Clear menu cuts across the sides of the ball, not the storage layers, so the domain gained its own erasure vocabulary rather than reusing the layer clear that already existed. Routes takes the concept off and Coverage takes the call off, both leaving their players standing so the next concept is drawn from the same formation; Offense and Defense take a side away entire with its lines, its notes, and its Assignments; Text takes every note whichever side wrote it; All empties the field. Which side a line belongs to is decided by the man running it, because that is what a Coach sees, and only where the model has something the original never did — a special-teams man, who plays both sides inside one Play — does the drawing itself answer instead: a drop, a blitz, or a stunt is the call and everything else is the concept. A note with no side of its own counts as offensive, matching the original. Every erasure is one ordinary undoable transaction that composes the same dependent cleanup an ordinary delete uses, so a note pinned to a cleared route and an Assignment naming a cleared Player go with it, and a note that both hangs off a cleared route and was marked defensive is removed once rather than twice. An erasure that would take nothing is no command at all, which is what greys the button offering it: the original's stated rule is that grey and inert come from the same number, and here they come from the same answer. The seven erasures the command palette had listed as unavailable are now real, and the dead Clear button on the tool rail opens the original's six-button menu with its own wording. Fixing the button's icon exposed a parity gap that a placeholder glyph had been hiding: with the correct eraser drawn, the rail's bottom group proved to sit thirty pixels too low, because the status bar had been nested inside the field's own column instead of running the width of the window under the rail and the inspector, where the original puts it. Moving it there put every rail button on the original's own pixel and brought all six parity ratchets down — the Editor from 1.8624% to 1.7750%, and the rest with it. Twelve domain tests mutation-checked against six reintroduced defects, and Chromium and WebKit/iPad prove that clearing the concept leaves eleven men standing, undoes as one step, and that a Clear with nothing left to take cannot be pressed. Assignments, reads, and conversions landed on 2026-08-09, closing the largest remaining item: selecting a route now offers the original's Coaching section — the read number, the Assignment, the conversion, and the coaching point — at the original's own field lengths, which are what fits on a card rather than what the model can hold, so a Play read back from anywhere else cannot fail validation for running long. Where each of these lives was the question the slice turned on. The original hangs all four on the route; ADR 0011 gives the Coach's wording to the man rather than to the line, and the v2 migration had already moved it there. Both are honoured by keeping the wording on the Assignment and reading back which line it is about through the movement action naming that route — which is also what lets one man carry different words for his route and for his alternate, as the original let him. An Assignment the Coach attached to something else is never overwritten: a route with no wording of its own gets a new one. Emptying the words takes the Assignment with them unless he has built structured actions onto it, in which case only the wording goes. The read order, the conversion, and the coaching point describe the line rather than the man and ride with it, so they are fields on the route; clearing one drops the key rather than storing an empty string, so a route he has emptied hashes like one he never wrote on. The read number and the Assignment print on the field as the original prints them — the number circled in the selection blue to one side of where the line finishes, the words stacked the other way with the Assignment shouting in mono above the conversion and the note. Those offsets are the original's own canvas pixels scaled into our frame, which draws the same field about eight per cent larger, so each mark sits the distance from the line the original put it rather than a fixed count of pixels nearer. Typing coalesces into one undo entry until the Coach leaves the field, and each input renders from a transient draft for the same reason a note's text does. Fifteen tests mutation-checked against twelve reintroduced defects, and Chromium and WebKit/iPad write all four fields, prove they survive a reload, and prove that emptying the wording takes the whole Assignment. That last browser test first passed against its own mutation and was strengthened: the mark leaving the field cannot tell an emptied Assignment from a removed one, because both stop it drawing, so it now reads what the Coach can undo, which says which happened. Deferred with reasons recorded: the original's global text-layout pass that resolves collisions between every mark on the field, its density presets, and the printed install and quiz pages that consume these fields. Alternates and choices landed on 2026-08-09, and with them the Player panel that holds them. Selecting a man now opens the original's Player panel in place of the idle one — the six shapes he can be drawn with, his fill, his colour, the letter on him and the tag under it, then every line he has, named the way the original names them: the first is the base stem and the rest are his alternates, each read out with how it is drawn and how many ways it forks. An alternate starts over at his stance — a different call he could be asked to run, drawn dotted so the base stem still reads first — and it is shaped like the last line he has, shifted toward the sideline he lines up on and pushed downfield, at the original's own 34 and 36 canvas pixels converted on the axis each belongs to. It carries that shape and nothing else: the bends, the per-segment styles and the endings of the line it came from are left behind, because this is another call rather than a copy of that one, which is what the original's own point-by-point rebuild amounts to. A man with nothing drawn on him gets the plain twelve-and-a-half-yard stem instead. A choice forks the same stem at the break the Coach picked — one release, then he reads — and where it points is worked out in the original's own canvas, because that is the frame the turn was measured in: an angle taken in yards would come out somewhere else entirely, the field being 1.525 times denser across than it is deep. One divergence is recorded where it is made: forking at the end of a line, the original turns off a leg of no length and so sends every such fork the same way whatever the route was doing, where here the leg that reaches the break stands in for the one that would leave it, so a fork off the end continues the line it grew from. Adding either follows the Coach onto what he just made, the way the original narrows to it, and removing a choice hands him the whole line back. Flip turns a line the other way without redrawing it, and the Player panel's Flip turns every line a man has about his own stance in one entry; neither rounds on the way through, the way the domain's own mirror leaves the arithmetic exact, so turning twice gives back the line rather than a rounding of it. Who is on the line is now a domain question beside the zone classification and the label meanings, and it is answered the way the original answers it — an offensive man with no letter on him, standing level with the ball — because a formation carries positions, not roles, and that is what decides whether he is offered a route or a block. Seventeen machine tests mutation-checked against twenty reintroduced defects, three of which exposed tests that could not have failed: a sideline test whose stem never reached the sideline, a removal test that took the first of two choices and so could not tell a filter from a slice, and a shape test whose mutation was stripped by the very helper it passed through. Reworking the first of those found a real defect of our own: the new stem's tip was rounded after being held inside the sideline, which put it back over by three ten-billionths of a yard — the same ordering hazard the drawing code already guards against, met again in a new place. Chromium and WebKit/iPad give a man a second line and prove it undoes as one step, fork a stem and take the fork away again, and turn every line he has the other way. A third browser defect was the field reflowing when a panel closes, so a point measured before the panel changed lands somewhere else — the same trap as the reload case, and re-measured the same way. Deferred with reasons recorded: the Quick route and Quick block preset menus on each line, which need the original's route catalogues (Phase 4.4), and the rest of the original's Player panel — Duplicate, Mirror, Delete and the whole-line scope buttons — which the contextual actions slice picks up. Contextual actions closed 4.3 on 2026-08-09. Right-clicking a Player or a route, or holding a press on one, opens the original's five-item menu where the pointer is — Duplicate, Mirror, Bring forward, Send back, Delete — held clear of the far edges at the original's own margin so a menu asked for in the corner is still whole. Asking about something settles the selection the way the original settles it: what he already picked stays picked, so asking about one of several does not throw the rest away and Delete still takes the group, and anything else becomes the selection on its own. That rule is the machine's rather than the shell's, because it is a selection change arriving by a new modality and ADR 0016 puts every modality through one model; the shell's part is only where to draw the menu. Whatever was being drawn is abandoned, since the Coach has stopped to look. ADR 0016 also forbids a menu holding anything a keyboard cannot reach, which is what made Bring forward and Send back real rather than listed: they now walk a line through its layer one step at a time, skipping a neighbour that is also picked so a group keeps its own order as it travels, and walking the list from the back on the way forward so two picked lines cannot leapfrog. Players are not in it — they draw above every line whatever order they are stored in, which is why the original reorders only routes and text, and why on a Player both items are greyed from the same answer that would run them. How the reorder reaches the document is the domain's existing answer rather than a new one: a layer whose order changed is replaced wholesale rather than guessed at with a move script, so the step is expressed as an ordinary difference between the Play and the Play reordered, and the policy that was already written and tested is the one that runs. Fourteen machine tests mutation-checked against fourteen reintroduced defects, one of which showed a group test that could not tell the neighbour guard from its absence until the group was pushed against the back of its layer, where the only neighbour left is one of itself. Two defects of our own surfaced in the browser and neither was reachable from a unit test. The long press asked whether the press had become a drag before dispatching the move that makes it one, so a single decisive movement left the menu armed under a man already being dragged; it is asked after now. And the press hit-tested with mouse precision, which would have made this menu the one thing on the field a finger had to be accurate to open; it asks with the modality that pressed. One divergence recorded: the original allowed six pixels of tremor before giving up on the menu where production lets go at the two the machine already uses, because one press cannot both be dragging a man and offering a menu about him, and the machine is what decides which is happening. Chromium and WebKit/iPad open the menu with a right-click and delete through it, prove a Player's reorder items are greyed and that Escape closes the menu without losing what he picked, bring a line forward from both the menu and the keyboard, and prove a held press opens the menu where a tap and a drag do not. Noted for 4.5 rather than fixed here: hit tolerances are measured in the drawn frame rather than in screen pixels, so a coarse pointer is given a smaller physical target on a smaller screen — the wrong direction, and a touch-target question rather than a contextual-actions one. Phase 4.3 is complete.
    [x] 4.4 - Implement every original Formation, defense, Concept, scope, preset, mirror, camera, panel, mode, palette, and help workflow
        In progress — the Formations landed on 2026-08-09, and with them the first piece of Phase 4 that is mostly domain data. The eighteen sets the original ships with are now Formation entities in the domain, written in the canvas pixels it drew them in so every number can be read straight off the frozen specification, and converted once on the axis each belongs to. Nine are described strong to the right and each one's left-handed twin is derived by reflection rather than written out again, because a Coach who tightens one expects the other to be the same set the other way; reflecting trades the names that say which side a man plays — the X and the Z change places, and so do the tackles and the guards, since a left tackle reflected is a right tackle. A set is a football object rather than a bag of coordinates: it is described from the ball it aligns to and names a position for every man in it, which is what lets a route survive a realignment, because the Coach who put a dig on his X wants it on his X afterwards wherever the new set stands him. Which position a man is playing is answered the way the original answers it and now sits in the domain beside who is on the line: his letter first, since X, Y, Z, H, Q and F are unambiguous, then where he stands — the five unlettered men nearest the ball are the line, named left to right and centred on the names when there are fewer than five, and the rest are backs or slots by how deep and how wide they stand. Where the ball is spotted is read off the men rather than stored, so splits are measured from the centre and a set moved to a hash keeps its shape. Applying a set moves the men in place and carries what belongs to each of them: every route travels with the man running it, bends and forks included, and an unbound note beside a man goes with him, where one pinned to a route needs no help because it already rides what it is pinned to. Slots nobody plays bring a man on, men the set has no place for stay exactly where they are rather than being made into something they are not, and the whole thing is expressed as an ordinary difference between the Play and the Play realigned — the domain's existing answer for a layer that changed, reused rather than replaced — so it is one hash-guarded transaction and one press of undo whatever it touched. Which set is on the field is two questions with two different tolerances, as it was in the original: recognising one allows fourteen pixels and is measured from the ball, so a set moved to a hash is still itself, while confirming the set the Coach named allows three across and two deep, because a set he called stops being that set the moment a man is somewhere else — and that check is proportional side by side, so tightening the splits keeps the name while moving one man alone does not. What a set declares about itself wins over what its men read as, which is why the I-Form stays 21 personnel although its second back is lettered H and reads as a slot, and why Gun Ace stays Right although two tight ends split either side are nobody's strong side; the reading is a fallback for a set saved without a name, not an authority. The browser itself is the original's: search, personnel and set filters, cards grouped the way a call sheet lays them out, and a thumbnail on each card scaled into its own bounds in the frame the original measured it in, because a Coach reads the picture before the name. Hovering a card lays the set over the field in grey so he can compare it with what is already there without leaving the list. Picking one is a decision rather than a list selection — it applies and gets out of the way — and what happened is said where he is already looking, with one undo within reach: how many moved, how many routes were carried, who was brought on, who was left in place. The idle inspector's Formation section now names the set on the field, or says Custom alignment when the men are nobody's stock alignment, which is what the seeded Play is. Twenty-eight domain mutations and ten shell mutations were reintroduced and all caught, but only after six tests were strengthened that could not have failed: a line test whose extra men stood outside the reach the line is picked from, so nearness to the ball and side of the ball gave the same five; a ball-spot test where the centre and the middle of the line were the same place; a note test whose pinned note stood too far from any man to have been adopted anyway; a recognition test that never had the wrong number of men on the field; a tightening test that stayed inside the bounds it was meant to be testing; and a personnel test that asserted the declared value on a set where the declared and the read value agree. Three test premises were also wrong about the original rather than about production and were corrected to what it actually does. Chromium and WebKit/iPad put the men in another set and prove the routes are carried and that all of it undoes as one step, prove a set that needs a position this Play has not got brings a man on and hands him over ready to adjust while the man it has no place for stays put, and prove the panel names the set once it is taken and the ghost appears and leaves with the pointer. Two defects of our own surfaced. The command palette and the shortcut panel have both listed undo and redo since they were built, and only the header buttons ran them — the keyboard now does, left to the browser inside a text field where undo means the words rather than the Play. And the field's own wrapper was not a positioning context, so the first thing placed inside it landed against the whole stage instead and covered the timeline; on a smaller screen that put the toast's Undo out of reach. One browser trap was met for the third time and answered the same way: a point measured in screen pixels before a panel grows lands somewhere else after it, so the test compares where a man stands on the field rather than where he is on the glass. The new overlay's parity ratchet is recorded at 2.0292 per cent, above the Editor's own 1.7750 for one reason stated where it is set: the original's browser carries an All / Favorites / Mine tab strip and a footer that saves the offense on the field as a set of its own, and both need somewhere to keep a Coach's own formations, which production has not got yet. Deferred with reasons recorded: saved and favourite formations and the tab strip and footer that reach them; the ball-on-hash buttons, which move the whole Play and belong with the camera work; and the jet rule, which is a route rule rather than a formation one. The defenses followed on 2026-08-09, and they are where a call differs from a set: a formation is an alignment, but a defense draws itself. The eleven calls the original ships with are now domain data, each one a Formation of eleven defenders together with the lines they run, and each line named by the slot of the man running it rather than by his place in a list, which is the original's own storage detail. A defender is drawn as his letter and nothing else, which is what tells a Coach at a glance which side of the ball he is looking at, and because the same letter is played twice — two corners, two ends — the side each of them plays is what tells the two apart. Putting a call on the field brings the men on and draws what each of them has to do: a zone drop is dashed, ends in a bubble, and owns ground, where a man assignment is the same kind of line drawn dotted to an arrow and owning none — which is exactly the difference a Coach sees between covering grass and covering a man. How much ground a drop owns comes from how deep it ends, at the original's three sizes, and each size is a lateral radius and a depth radius written in the frame of the axis it belongs to rather than both read on one. What kind of zone it is then falls out of the reading the domain already does for a drop drawn by hand, so a call's drops and a Coach's own are classified by one answer rather than two. Only one defense can be on the field at a time, so a new call takes the whole of the last one with it — its men, every line they were running whether or not it was the call's own, and the call text that named it — and a note pinned to a line that has gone goes with it rather than being left pointing at nothing. One divergence is recorded where it is made: the original's own reading of which side a line belongs to counts a stunt as defensive, but the swap forgets it and leaves one behind, so production uses the reading it already has in both places. Which call is on the field is a simpler question than which set is, because a defense is placed rather than realigned onto the men already there: there is nothing to pair up and no proportional reading to make, so the men are compared with the call exactly, letters and all, as one sorted string of who is standing where — which leaves no bookkeeping to get wrong and no man who can be counted twice. Two guards were removed as dead rather than left untested: the early return for an empty field, since nobody on it already reads as no call, and the used-man bookkeeping the string comparison makes unnecessary. The browser is the original's, narrowed by front and by coverage, and picking a front regroups the rest by coverage because with one front chosen the useful heading is no longer the front again. Its cards draw the front and the secondary in their real relationship over a faint offensive line, since a front only means anything against the blockers in front of it, and with assignments on they show the very lines that will land on the field, bubbles and all, in the colours the field itself uses — a Coach reading the card and reading the field are reading the same picture. Assignments are off to begin with, as the original has them: a call dropped in as an alignment is something to draw your own coverage on top of. Getting that wrong is what the parity harness caught — it had been on by default, and the measured gap fell from 1.78 to 1.65 per cent once it was off, which is the lowest of any state and below the Editor's own. Eighteen domain mutations and eight shell mutations were reintroduced and all caught, after three tests were strengthened that could not have failed: one whose two radii differed under the mutation as well as without it, one where every line a replaced defender ran was already a defensive line and so was taken by a different clause anyway, and one that moved a defender sideways where only sideways was being checked. Chromium and WebKit/iPad put a call on as an alignment and then again with the assignments on, prove the toast says which and that all of it undoes as one step, and swap one call for another to prove eleven men and seven lines leave together rather than staying underneath. Deferred with reasons recorded: favourite calls and the tab strip that reaches them, alongside the saved formations they share a home with. Concepts and the route tree followed on 2026-08-09, and they pay off a deferral from 4.3. A preset is a shape measured from the man's own spot rather than a place on the field, so the same call lands correctly on a variation that lines him up somewhere else, and its across-the-field numbers are signed by which way is outward for the man running it, so one entry covers a receiver on either side of the ball. The ten shapes are now domain data, written in the original's canvas pixels and converted on the axis each number belongs to; the wheel is the only one that bends, and it carries its own control points. Which way is outward is the man's rather than the line's, which matters when a call is run on from the end of what he already has: a shallow crosser from the left ends up on the right of the ball, and an out run on from there still breaks to his own sideline. The Quick route picker sits on each line row in the Player panel, where a Coach is already looking at the line, and it is offered on a route and on nothing else, because a block, a motion and a drop are shapes of their own with their own calls. Redrawing a line remembers which call it is, so the picker says so rather than forgetting, and follows him onto the line he just changed; running one on from the end of what he has turns that end into a real break, and stops naming the line as any one call, since it is no longer one. A fork hanging off a break the new shape no longer has moves back to the last break there is rather than pointing at nothing. A concept is a distribution and not a route: every man it is about is given his job by the position he plays, mirrored to the side he lines up on, which is the reading the formations slice had already put in the domain — this is the slice where that pays for itself. Who a concept is about is the men who could run a route, so a defender and a lineman are out, and being a lineman is read off where a man stands rather than off the position he was given, which is what keeps an extra tackle out of the distribution although a sixth man on the line reads as a slot. Whether the quarterback has a job is left to the concept rather than guarded against, since none of the ten gives him one and a screen that did would be naming him deliberately. Each job is drawn either as a call off the tree, in which case the line remembers which, or as the concept's own shape where the shape belongs to the concept rather than to the tree — the mesh crossers being the clearest case. A job ends the way the job ends rather than the way a route usually does, so a stick sits down and looks back. The words each man is told are written as Assignments on the man, which is where ADR 0011 puts them, and the concept owns the ones it wrote: deleting a line a Coach drew leaves his wording standing, because the words are his and the line was only what they were about, but a concept says what each of these men does, so the words it replaces go with the line they described — which is what the original did by keeping them on the line in the first place. Asking for the concept already on takes it off again, which is how the same button puts it up and pulls it down, and a concept with nobody to draw it on is no command at all, which is what greys the button offering it. Twenty-two editor mutations and five shell mutations were reintroduced and all caught, after six tests were strengthened that could not have failed — among them one where the anchor and the man were on the same side so handedness could not be told apart, one that could not tell a continued line from one with its anchor written twice, and one whose extra men stood where the concept's own role list already excluded them. Two defects of our own were found by the tests before they were found anywhere else: a concept's wording piled up rather than being replaced, because the domain's delete deliberately leaves a Coach's words standing when his line goes; and the start of a redrawn line was rounded off the man it starts on, which is the same ordering hazard the alternates slice met, in a third place. Chromium and WebKit/iPad draw a whole concept and take it off with the same button, prove both directions undo as one step each, and redraw one man's line off the tree and prove the picker remembers which call it is. Blocking and the defensive calls closed the catalogues on 2026-08-09. Twelve block shapes and eleven defensive ones are now domain data beside the route tree, drawn from the man's own spot the same way — with one difference that belongs to blocking: a handful of calls carry a real field direction rather than mirroring about the ball, because Set left means left whichever side of the centre a man lines up on. A pull bends and starts by dropping off the ball rather than going forward, and the breaks the original marks with a tick are marked. A defensive call carries the ground it owns where it owns any, at the original's own radii, each written in the frame of the axis it belongs to; a man call and a rush own none, which is the same reading the defense browser already relies on. Where a route is reshaped in place — keeping its forks and everything the Coach wrote on it — a block or a drop replaces what the man was doing, because it is a whole call rather than a shape, and the two go through different commands for that reason. A defensive call replaces every kind of defensive line rather than only its own kind, since a drop, a rush and a stunt are all the same call. The Line call section now puts one of six calls on the whole line at once and takes it off again with the same button, and a call only comes off when every man is already running it — asking for one that some of them have puts it on the rest. The selection is deliberately left alone when a line call runs, because picking the line would swap in another panel and unmount the very buttons just pressed, which is the original's own note. The Quick call picker on each line row now offers the calls that belong to that kind of line: the tree on a route, the blocking calls on a block, the drops and rushes on a defender's line, and none at all on a motion or a ball flight, which have no catalogue of their own. Sixteen editor mutations and four shell mutations were reintroduced and all caught, after one test was strengthened that could not have failed: it only ever gave a call to the whole line at once, so it could not tell a call coming off from a call going on to the men who had not got it. Chromium and WebKit/iPad give the whole line a call, replace it with another, take it off, and prove it undoes as one step. Spotting the ball landed on 2026-08-09, paying off the deferral the formations slice recorded. The ball sits where the official puts it — left hash, middle, right hash — and the whole Play travels with it, defense included, with the splits kept exactly as drawn. Where a hash leaves no room for them the boundary side is squeezed rather than allowed to stand out of bounds, and the field side is given back some of what the boundary side lost, capped so nothing ends up looking stretched; a margin is kept off the paint so the widest man has grass under him rather than standing on the minimum, which is the case a test had to be written for on purpose, since a set that just fits without the margin is exactly what the margin exists to catch. Which spot the ball is on is read off the men rather than stored, and a ball between two hashes is named as neither. One divergence is recorded where it is made: the original clamps a break that would land out of bounds, and then has to remember the line's true shape so that moving the ball back gives it its length again; production does not clamp, so nothing is lost and there is nothing to remember — the same information expressed by not destroying it. A move that needed no squeezing is therefore exactly reversible, and a squeeze is not undone by moving back, which is the original's behaviour too, because a squeeze is a change to the splits rather than a change of spot. The spot the ball is already on is no command at all, which is what greys the button offering it. Fourteen domain mutations and four shell mutations were reintroduced and all caught, after six tests were strengthened that could not have failed — among them one where the centre was drawn, so reading the spot off the defense as well made no difference; one where both sides of the ball happened to scale alike, so the two ratios could not be told apart; and one where a note's leader was carried and placed to the same place because the move was a pure translation. One test claim was simply wrong about the design and was corrected: moving the ball out and back is the identity only when nothing had to be squeezed. A lint rule about reading refs during render was what prompted moving the concept, line-call and ball-spot handlers off the render path entirely, so each is built from the live Play the way the reorder shortcut already was; the concepts and line calls had been passing only because the rule could not see through the object they were built into, which is a blind spot rather than a licence. Chromium and WebKit/iPad spot the ball on a hash, prove the men and their lines travel together, prove the toast says when the splits had to be tightened, prove the spot already on is greyed, and prove it undoes as one step. The camera landed on 2026-08-09, closing the last substantial piece of 4.4. Where the Coach is looking is not part of the Play — panning is not an edit and none of it is undoable — so it lives beside the interaction model rather than in the document, and its rules are a pure module of their own: fit, clamp, zoom about a point, pan, centre, and fit to a rectangle. The scene is still drawn in the one frame the renderer has always drawn it in, and the camera is a rectangle of that frame shown on screen, which is what keeps the parity goldens still: at fit the camera is the whole frame, so the picture is the same picture. A wheel pushes the field in and out about the pointer, which stays where it is while everything else moves past it; a trackpad swipe moves the field instead, sideways on its own and sideways from a held shift. Two fingers pinch and pan together, a held space or a held alt turns any drag into a pan, and the keyboard reaches fit, fit-to-selection and zoom either way. Fit to selection shows what he picked with room around it, widening rather than cutting off something taller than it is wide, and shows the whole field when he has picked nothing. He may push a little past the edge of the frame either way, so a man standing on the sideline can be brought off it rather than being pinned under the frame's own edge. The two things this had to get right are both about the difference between a frame unit and a screen pixel, and both are now measured: what a client pixel is worth in yards is read through the camera, so pressing where a man is drawn is pressing him however far in the Coach has zoomed; and a hit tolerance written in screen pixels is divided by the zoom, so a grab target stays the size his finger is rather than growing with the picture, which the handles do too. Twelve camera mutations and eight shell mutations were reintroduced and all caught, after three tests were strengthened or written for the purpose: the clamp's own zoom limit is unreachable through zooming, which has to know its width before it can hold the point under the pointer, so it is tested through the door every other caller uses; the pointer-through-the-camera rule needed a test that zooms first and then presses a man where he is now drawn; and the tolerance rule needed a press ten frame units off a man's middle, which is outside what a mouse is allowed at that zoom and inside what those same pixels would be worth if the camera were left out of the sum. The browser helper that converts frame coordinates to client coordinates now reads the live camera rather than assuming the whole frame, which is the same correction the shell needed and was wrong in the tests first. Noted at the time and since fixed: a WebKit/iPad browser test that had been seen to fail under full-suite load. The verbs the palette names closed 4.4 on 2026-08-09. Eight of them were listed and could not run; all eight are real now, and each is unavailable exactly when it would do nothing, which is the same rule the Clear menu and the reorder items already used. Flipping the strength is the mirror the domain already does plus the language that goes with it, so a card that read STRONG RIGHT does not end up describing the picture wrongly: the football words turn round — left and right, strong and weak, the tackles — in the case the Coach wrote them in, and every word that is not one of them is left exactly as it is, capital letters in the middle included. Those words are football rather than English, so they live in the domain beside the label meanings and the zone classification. The X and the Z trade letters, because a letter says which side a man plays. A set the Coach is recognisably in flips through its own named counterpart rather than by reflection alone, which is what puts a nudged set back on the twin's own spot instead of carrying the nudge across with it — the mirror pairing the formations catalogue recorded, paying off. Lining men up puts them all at the depth between them or spreads them evenly between the two widest, who do not move, and each man's lines travel with him the way they do when he is dragged. Grouping ties men, lines and notes together; picking any one of them picks the rest and dragging any one drags the rest, which is the whole of what a group is for, and it is done at the one place a selection is settled rather than at each of the ways he can arrive at one. Untying leaves a Play that hashes exactly like one that was never grouped. Reversing a line turns its points round and moves each bend back to the break it now arrives at, dropping the forks, since they hung off breaks counted from a start that is now the finish; one divergence is recorded where it is made, that the original also detaches the line from the man running it, which production's schema has no way to express, so it stays his — the same answer already given where a line is pasted without him. A depth marker pins the number to the leg he picked out and never to the stance, which is not a break. Twenty-two editor mutations and four shell mutations were reintroduced and all caught, after eight tests were strengthened that could not have failed — among them three where the fixture could not tell two answers apart: three men already standing at one depth, so lining them up moved nobody; a group expanded from the very thing that named it, so nothing had to be brought along; and a set whose named twin is exactly its own reflection, so flipping through the twin and reflecting gave the same picture until a man was nudged first. Two guards proved equivalent rather than untested and were removed: the depth number's whole-versus-fractional formatting, which for a half-yard value is the same string either way, and a case-preserving branch that was doing the work of the guard above it. The three-times-seen WebKit/iPad flake was chased down rather than waved through, and it was a test defect with a real cause: it typed into the read field and then blurred a different one, and typing coalesces until the Coach leaves the field he is in, so the last thing written was still in hand. Chromium committed it anyway and WebKit did not. Chromium and WebKit/iPad now flip the strength from the command palette, and tie two men together, prove that picking one picks both and that dragging one drags both from nothing picked, and untie them again. Fourteen palette entries remain unavailable: twelve exports and printed pages, which are Phase 9, and the two that make or branch a Play, which belong with the library. Demo sequences landed as 4.1 shell work on 2026-08-21, with the B1 identity reset on EditorStore.adoptPlay. Phase 4.4 is complete.
    [x] 4.5 - Complete keyboard, screen-reader, reduced-motion, touch, Pencil, and phone read-only behavior
        In progress — touch targets landed on 2026-08-09, closing the defect the contextual-actions slice recorded and could not fix there. Every rule the original wrote in pixels — how far a press may travel before it becomes a drag, how near a line has to be pressed, how far apart two breaks must be, how big a handle is — is a rule about the Coach's finger and his screen rather than about the drawn frame. Production had been measuring all of them in frame units, which are only pixels when the field happens to be drawn at its own size, so a smaller screen quietly handed a coarse pointer a smaller target: the wrong direction, and exactly backwards from what ADR 0016 asks. How many pixels one frame unit is really drawn at is now measured off the element itself and watched for resizes, and every one of those rules is read through it — so forty-four pixels means forty-four pixels on a tablet, and the same press means the same thing on a phone as on a desk. The camera's own zoom falls out of the same measurement rather than being a second half-answer beside it. The browser proves it on the glass: the smallest grab target on the coarse-pointer device measures the forty-four pixels ADR 0016 asks for, which is a measurement of the rendered box rather than of the arithmetic that produced it. One browser test had to be corrected rather than the product: it started its marquee on grass that is no longer grass, because a finger is now owed a good deal more of the frame than it was. Chasing the last of a three-times-seen WebKit/iPad flake found a real defect underneath it, and not in the accessibility work: a command carries whole entities, and saves are serialised, so a command built in the shell from the Play as it was when a keystroke was handled can be a save out of date — and applying it silently puts back the field the save before it had just changed. Typing quickly into two coaching fields of one line lost the first of them, intermittently, depending on which save landed first. The store now takes an edit as something to build rather than something already built, and builds it inside the save queue against the Play it will actually land on; every field a Coach types into quickly goes through it. Three store mutations and three shell mutations were reintroduced and all caught, and the browser suite ran clean three times over where it had been failing about one run in three. Reaching the field without a pointer followed on 2026-08-09. The picture stays a picture — one image with the Play's name, which is what it is — and the same field is also an ordinary list of everything on it: his men first, then what each of them is asked to do, then the notes, each one a button that picks what it names. Ordinary buttons rather than a focus trap laid over the drawing, so the tab order a screen reader already gives him is the order he reads the Play in, and Escape is not something he has to know. Each of them is off screen until it takes focus, at which point it comes into view, so a Coach working by keyboard can see where he is and nobody else ever sees it. Every line now has a name of its own, saying the football rather than the geometry: the man running it, what kind of line it is, what he is told to do, and where it falls in the read. What is picked is said out loud as well as haloed, and it says how many when he has picked more than one. Wiring that up found a defect that had nothing to do with the field: the editor's own keyboard handler took Enter and Space for itself, which left every button in the application dead to anyone who had tabbed to one — the shortcut panel could be opened by clicking it and by nothing else. A control the Coach has tabbed to now keeps its own activation keys, with one exception stated where it is made: a route in progress takes Enter back, because pressing the field does not move focus off whichever tool button he started from, and mid-route that key is plainly aimed at the route. Two more defects of the same family were found under the last of the WebKit flake: a field's blur can arrive after the Coach has already moved on and typed into the next one, and clearing the single draft slot then wipes what he has just written out from under him. Only the field being left now lets go of the draft, in the Coaching panel and in the Player panel both. What remained after that was a race between a harness that sets three boxes with no gap between them and a controlled input, which is a trick rather than a gesture: the browser test now finishes each field before starting the next, which is what typing coalescing until the Coach leaves the field he is in actually describes. Seven shell mutations and one render mutation were reintroduced and all caught, and the whole check ran clean three times over.
        Complete on 2026-08-13 with the Pencil and the phone. An iPad answers `(pointer: coarse)` whichever pointer the Coach has picked up, so the field had been handing an Apple Pencil a fingertip's targets and losing exactly the precision he reached for it to get; it now goes by what actually touched it. A stylus module beside the interaction machine keeps the pointer in hand and answers what depends on it, and grab targets are sized from that rather than from the device — the Pencil gets the original's precision on the tablet that calls itself coarse, and a finger keeps its forty-four pixels. With the Pencil out the hands change jobs, which is what ADR 0016 means by leaving touch the viewport: while the tip is on the glass every other contact is the hand holding it and is refused outright, a palm that landed first has whatever it had started thrown away when the tip arrives and is taken off the books so the next finger down is one finger rather than the second half of a pinch with a hand that is no longer there, and a press the field refused cannot end one it accepted — which matters mid-route, where the machine releases a drawing's press without asking which pointer sent it. Once a Pencil has been used a finger moves the field instead of what is on it; before that it is the only pointer he has and keeps doing everything. A pinch that loses a finger now goes on panning under the one still down, picked up where that finger actually is, and a third finger joins the pinch rather than reaching for whatever it landed on. A screen too small to work on shows the Play instead of the editor, and where that line falls is written once in the editor package so the shell and the stylesheet cannot disagree about it: the editor wants a field at least as wide as the panels beside it and deep enough for a backfield and a stem, and a screen short of either — a phone held either way up — reads instead. That view has the Play, its name, and the outline every man, line and note is named in; it has no rail, no inspector, no menus, and no undo. Every pointer on it moves only the camera, so a thumb dragged across a man moves the field under him, and the keyboard's shortcuts are off, because what they reach is a Play the Coach cannot see he has changed. Turning the phone over is watched rather than read once, a screen he can only read opens on the Play whole, and a close look he took on it survives picking the tablet back up. Fourteen stylus tests, four screen tests and eight browser gestures cover the two, proven non-vacuous against thirty reintroduced defects. Six guards that could not change any outcome came out rather than being given tests: the pointer id on a pan, the palm check on pointer move, the release the machine already ignores, and the read-only guards those two shadowed. Two of the defects were only caught after the fixtures were sharpened — a palm sliding back the way it came, because the camera was pinned at its clamp and hid the difference, and a third finger landing on a different man than the first, because the first finger's press had already picked up the one under it. The forty-four-pixel test now drives itself with a finger rather than with a mouse: it had been asserting what a finger is owed while pressing with the one pointer that is not a finger, and passed only because the device was answering for it.
    [x] 4.6 - Prove editor frame-rate and input-to-paint budgets on target devices
        Evidence: pointer movement no longer clones the Play or rebuilds the React field. A paint loop coalesces pointer events onto animation frames, patches only the SVG that moved — the Player, the routes that travel with him, and any bound note — and keeps snap guides, the marquee, and handles on a live overlay that re-renders without the shell. React commits of the field during a 24-step drag stay in the single digits rather than growing with the number of moves, which is the defect finding #13 described and the architecture ADR 0002 and ADR 0015 required. The loop classifies p95 frame interval against 16.67 ms (60 FPS) and p95 input-to-paint against 50 ms; those classifications are written on the field the way local save writes data-save-within-budget, always against the Coach-device budget even when CI runners cannot meet it. Unit tests cover coalescing, discarded-event input-to-paint, p95 versus mean (a hitch that still averages near 60 FPS is not sustained 60 FPS), affected-entity selection, and DOM patching of only the moved nodes. Chromium at 1440×960 drives a real drag and asserts both strict budgets on a developer machine. WebKit/iPad asserts the architecture (React commits stay in the single digits) and the input-to-paint ceiling the same way; it does not gate a numeric frame p95, because Playwright WebKit is not a 60 Hz panel. Shared CI runners and hosted agents use the 8× ceiling for Chromium frame p95 and for input-to-paint on both browsers, the way item 3.2 already does for acknowledgement. Physical ninth-generation iPad and five-year-old laptop evidence remains a Phase 12.3 / 10.4 release gate.
[x] 5.0 - Phase 5 - Deterministic animation and playback
    [x] 5.1 - Implement timing authoring for pre-snap motion, routes, segments, and coordinated Play duration
        Evidence: each route carries optional stored delay/speed/hold; unset fields resolve from role and shape the way the original does — linemen and blocks slower, zone drops delayed and held, backs a half-beat, a hitch sitting down 0.6s — and the first Timing edit persists the resolved defaults so a later change does not drop the sit-down. Motion is planned before snap (negative milliseconds) and finishes together with a gap; snap stays 0. Distance along the route, sampled on the same Bézier the renderer uses, drives position at constant pace through a break. Timing is integer milliseconds and survives mirror (the double-mirror identity holds). The Route inspector offers Delay / Speed / Hold with the original hint. Playback never writes the saved Play.
    [x] 5.2 - Implement deterministic play, pause, restart, speed, and scrub controls
        Evidence: a 34px bar under the field — play/pause, scrubber with a 1px snap tick, clock, 0.5× / 1× / 2×, reset — appears only when the Play has a route. The clock is isolated from React: play, pause, seek, rate, and reset update state; while running, requestAnimationFrame samples absolute wall time (ADR 0028 — no frame-to-frame dt) and patches only player transforms and ghost-trail path data (ADR 0002 / 0015). At rest the field is the static diagram; once a frame is asked for, routes fade to 18% and solid ghost trails stay behind the men. Play at the end restarts from the timeline start. Reduced motion jumps to the end. Space plays and pauses; a held space still pans. Field pointer-down while playing pauses so a frozen frame stays editable.
    [x] 5.3 - Share one playback evaluator between editor and read-only presentation
        Evidence: planPlay / evaluatePlayAt live in the domain; buildRenderScene({ atMs, playing }) is the only scene builder; Editor and Present mount the same PlaybackBar and patch the same SVG. Frame-sequence PNGs plus a snap-relative manifest, and a four-frame Snap / First break / Throw / Finish progression strip on letter landscape, consume that evaluator. GIF and MP4 stay out of scope. Share Links are not built yet (Phase 8) and will import the same functions.
    [x] 5.4 - Verify deterministic timestamps, offline responsiveness, keyboard control, and reduced motion
        Evidence: unit tests pin hitch/back/lineman defaults, pre-snap motion, constant-pace breaks, absolute-time ticks, seek ≡ uninterrupted playback at the same timestamp, reduced-motion jump-to-end, rate changes that keep the current time, stored timing through a double mirror, rest-vs-animated scenes, and the export manifest/strip. Component tests drive the bar on the seeded Play. Chromium at 1440×960 and WebKit/iPad play Stick — Thunder, prove a receiver leaves his stance, prove a frozen frame still opens Timing on X's route, prove Reset returns the stance, and prove space plays and pauses in Present. Lint and typecheck are clean. Offline is unchanged: the clock and evaluator are local and do not wait on a network.
[x] 6.0 - Phase 6 - Playbook library, organization, and retrieval
    [x] 6.1 - Preserve original library, Concept tree, variation, scope, propagation, detach, save, rename, delete, and transition behavior
        Evidence: the original inspector library remains — Concept tree, Applies to (this play / whole concept / Pick…), + Variation, note/tags, push, detach, delete-with-confirm, Save flash, and Browse Playbook as an additive overlay. Switching Plays waits out the save queue and refuses a failed local save (parity-matrix B2). Scoped route work broadcasts with skip copy (`Applied to 4 of 5 — Red zone has no Z route`). Present steps the family (`1 / 5 · GUN DOUBLES LEFT`) with ← →. Stick — Thunder ships with its four variations plus the demo-tour examples.
    [x] 6.2 - Implement Formation and Field Profile management and explicit reapplication
        Evidence: the inspector Field section lists playbook and built-in profiles below the original Formation / Line call / Concept / Defense / Library stack; applying one is `set-field-profile` and never rewrites yards (ADR 0032). A Play whose copied profile or Formation source revision is behind offers Reapply. A Coach can keep the current markings as a named playbook profile, optionally as the default.
    [x] 6.3 - Build virtualized paged metadata lists/grids with focus and scroll restoration
        Evidence: the Playbook overlay virtualizes a 4-column card grid with TanStack Virtual, restores `library.browser.v1` scrollTop and focused Play, and filters by unit and Play Type without loading Play revisions.
    [x] 6.4 - Build Worker-based local search projections, filtering, and disposable indexes
        Evidence: search projections stay metadata-only; `searchPlays` token/prefix/Levenshtein-1 matching answers a 2,000-Play query inside 50 ms; a module Worker runs that path with a main-thread fallback. Clearing derived data rebuilds projections automatically.
    [x] 6.5 - Build lazy cancelable revision-keyed thumbnails
        Evidence: a card's picture is `playId:revisionHash:rendererVersion:fieldProfileRevision:theme`, generated on idle as an SVG blob, stored in the thumbnails table, and aborted when the card leaves the window.
    [x] 6.6 - Verify the 10-Playbook and 2,000-Plays-per-Playbook performance fixture
        Evidence: ten Playbooks list by metadata only; domain search of 2,000 Plays stays under 50 ms; the browser mounts a 2,000-Play catalogue without creating 2,000 DOM cards; Phase 3 already proved a commit into a 2,000-Play Playbook does not grow with Playbook size.
[ ] 7.0 - Phase 7 - Authentication, Convex replica, and conflict-safe sync
    [ ] 7.1 - Implement invite-only Clerk email-code authentication and optional passkeys
    [ ] 7.2 - Implement strict Convex schemas, authorization, validators, revisions, tombstones, and payload limits
    [ ] 7.3 - Implement durable idempotent push, cursor pull, acknowledgement, retry, and sync-head signaling
    [ ] 7.4 - Implement the non-blocking Conflict Inbox and all resolution paths
    [ ] 7.5 - Verify offline, intermittent, replayed, out-of-order, revoked-session, and two-device convergence cases
[ ] 8.0 - Phase 8 - Private assets and immutable Share Links
    [ ] 8.1 - Implement image validation, EXIF stripping, resizing, thumbnailing, hashing, and local-first storage
    [ ] 8.2 - Implement private content-addressed R2 synchronization with short-lived signed access
    [ ] 8.3 - Implement sanitized immutable Share Publications and curated Play sets
    [ ] 8.4 - Implement stable fragment-capability links, keyed hashes, revocation, republishing, expiry, and audits
    [ ] 8.5 - Harden the static share shell CSP and verify no secret or private-data leakage
    [ ] 8.6 - Implement external Film References without hosting or caching video
[ ] 9.0 - Phase 9 - Authoritative static coaching outputs
    [ ] 9.1 - Implement deterministic SVG and high-resolution PNG export
    [ ] 9.2 - Implement field print, practice cards, call sheet, install, position, quiz/key, slide, wristband, scout, progression, and Playbook outputs
    [ ] 9.3 - Implement original frame-sequence animation export and manifest without GIF/MP4 expansion
    [ ] 9.4 - Verify every original Export action, pagination, overflow, fonts, accessibility, offline behavior, and golden fixture
[ ] 10.0 - Phase 10 - PWA lifecycle, offline UX, and device hardening
    [ ] 10.1 - Implement the Workbox injectManifest app-shell-only service worker
    [ ] 10.2 - Implement install, offline, sync, storage-health, recovery, and explicit-update UX
    [ ] 10.3 - Verify schema-compatible update, rollback, and cached-shell recovery paths
    [ ] 10.4 - Execute the supported browser, iPad, Pencil, touch, keyboard, and phone matrix
    [ ] 10.5 - Prove warm/offline and cold-online startup budgets
[ ] 11.0 - Phase 11 - Security, observability, backup, and environment operations
    [ ] 11.1 - Isolate development, PR preview, staging, and production service configuration
    [ ] 11.2 - Complete authorization, rate-limit, abuse, CSP, headers, secret, dependency, and supply-chain checks
    [ ] 11.3 - Implement privacy-scrubbed Sentry reporting and content-free operational metrics
    [ ] 11.4 - Implement daily Convex, weekly off-platform, R2 retention, and restore tooling
    [ ] 11.5 - Run and document a timed restore drill against the 24-hour RPO and four-hour RTO
    [x] 11.6 - Configure local main, Conventional Commits, private-package semantic-release, and a non-publishing dry run
    [x] 11.7 - Establish remote main, Bun-based PR and merge gates, protected-branch rules, and CI-driven GitHub releases
[ ] 12.0 - Phase 12 - Beta validation and controlled release
    [ ] 12.1 - Pass end-to-end critical journeys across normal, offline, reconnect, conflict, sharing, and recovery states
    [ ] 12.2 - Pass automated and manual accessibility, keyboard, screen-reader, touch, and Pencil gates
    [ ] 12.3 - Capture performance evidence at the full beta data scale on both target devices
    [ ] 12.4 - Document beta support, feedback, incident, export, deletion, rollback, and release procedures
    [ ] 12.5 - Resolve every severity-one durability, privacy, accessibility, and data-loss defect
    [ ] 12.6 - Obtain explicit approval and record any production promotion separately
    [ ] 12.7 - Close every prototype parity row or record its explicit approved exception with evidence
[ ] 13.0 - Phase 13 - Ship and archive
    [ ] 13.1 - Complete final verification, release notes, and deferred-work record
    [ ] 13.2 - On merge: move docs/chalk-beta-IMPLEMENTATION_PLAN.md + docs/chalk-beta-progress.txt to docs/archive/
