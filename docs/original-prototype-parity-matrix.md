# Original Chalk Prototype Parity Matrix

Status: governing inventory; item-level capture in progress
Canonical source: `Chalk Football Play Editor-2/Chalk Play Editor.dc.html`
Canonical runnable support: `Chalk Football Play Editor-2/support.js`
Decision: ADR 0039

## Rule

Every capability below is required in the deployable production app unless the product owner explicitly approves a change. Internal reimplementation is expected; to the Coach, the rebuild must be identical. Unapproved visual, textual, interaction, workflow, shortcut, touch, animation, or output divergence fails parity.

The one standing exception is the corrected-defect list immediately below. Those behaviors are approved divergences: reproducing them faithfully fails parity instead of satisfying it.

## Corrected expectations for documented prototype defects

The canonical prototype is the specification for what Chalk _does_, not for every way it currently fails. Seven defects in the original were verified against the running app and reproduced in a browser; the reviews in `docs/reviews/` carry the analysis and `file:line` anchors, and the fixes plus their 53 regression checks are preserved on the unmerged `fix/review-findings` branch as executable statements of the corrected behavior. That branch is not a merge candidate — the tracked prototype stays byte-identical to the archive — but its four test files are the behavior scripts this matrix's evidence section requires for these rows.

Where the corrected behavior is already delivered, the phase that delivered it is named. Where it is still open, the phase that owns it is named, and the correction is a requirement of that item rather than a follow-up.

| Defect                                                               | The original does                                                                                                                                                                    | The production app must                                                                                                                                              | Status                                                                                                                                                                                  |
| -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1 — demo handoff overwrites the active Play (**Critical**)          | Opening a demo into the editor leaves the previously open Play's identity attached, so the next Save rewrites and can rename unrelated work                                          | Treat a demo handoff as a new unsaved Play with no prior identity; saving it creates a record and leaves the previously active Play unchanged                        | Corrected — Phase 4.1 Demo/Tour with EditorStore.adoptPlay (Phase 6.1 identity seam)                                                                                                    |
| B2 — switching Plays discards unsaved work (**High**)                | Opening another Play replaces the working document silently; the displaced work survives only in the in-memory undo stack until the tab reloads                                      | Route every whole-document replacement through one guarded transition; unsaved work is never discarded without the Coach choosing to                                 | Corrected — Phase 6.1 (`openStoredPlay`, refuse switch on local-save error, wait out the save queue)                                                                                    |
| #1 — coverage zones absent from every output (**High**)              | Zones draw on the live canvas but never in export or print, so a Cover 3 prints as bare dashed lines and bubble markers                                                              | Zones render identically wherever the Play renders                                                                                                                   | Structurally prevented — Phases 2.3–2.4 project coverage areas from one `RenderScene`; Phase 9.4 owns the golden that proves it                                                         |
| #2 — saves fail silently while the UI says "Saved" (**High**)        | A quota-exhausted write is swallowed and the button still turns green                                                                                                                | Report the durable outcome truthfully and offer retry on failure                                                                                                     | Corrected — Phase 3.2                                                                                                                                                                   |
| #3 — lateral yardage ~53% too large (**High**)                       | Horizontal distances are divided by the vertical scale, so every lateral readout and zone width overstates; 45-degree snapping is 45 degrees on screen, ~33 on the grass             | Measure and snap in yard space on both axes; a snapped 45 is 45 on the grass                                                                                         | Corrected — Phase 2.2 for snapping, which chose grass-true breaks explicitly. The **conversion** into yard space had reproduced the same defect and was corrected separately; see below |
| #4 — live text and drag edits bypass commit (**High**)               | In-progress typing and pointer movement write the document without marking it unsaved, so the status bar lies and the history timer can produce zero snapshots across a long session | Separate transient gesture state from committed state; every completed gesture is exactly one commit and one undo entry, and save state is always truthful           | Corrected — Phases 3.2 and 3.3                                                                                                                                                          |
| B3 — an intentional clear is auto-undone on reload (**Medium-High**) | Startup treats any empty document as lost work and force-replaces it from history                                                                                                    | Auto-recover only unparseable storage; a structurally valid document — including a deliberately empty one — is preserved, and recovery is offered rather than forced | Corrected — Phase 3.4                                                                                                                                                                   |

### A corrected defect can still be reproduced downstream

Finding #3 was corrected in the geometry seam and then reproduced in the conversion that feeds it. `legacyCanvasToYards` read both axes on the depth scale, so every lateral coordinate it produced was 1.525 times too large, and the seeded `Stick — Thunder` carried that into its stored yards — the split end stood five yards out of bounds. A second defect hid it: field markings were drawn on a lateral scale the Players did not share, stretching the sidelines 177 pixels wider than the Play. The two errors differ by 1.3% and very nearly cancelled, so the Play looked correct until something moved.

Two lessons worth keeping:

- **A correction has to reach the data, not only the algorithm.** Phase 2.2's snapping was right the whole time; what was wrong was the Play it snapped.
- **Consistency assertions are not correctness assertions.** Checking that Players and markings agree passes while both are wrong. The guard that works pins the drawn field to its frame — an absolute, not a relationship.

The parity ratchets in `tests/parity/production-shell.spec.ts` rose 0.01–0.08 percentage points when this was fixed, because those goldens are captured from the uncorrected original. That rise is approved under this section rather than treated as a regression: matching the original here would be bug-compatibility, not parity.

The remaining reviewed findings (#5–#13, B4–B7) are not yet triaged into phases. They are defects in the original, not parity requirements; none should be reproduced deliberately, and each needs a decision before its owning phase closes.

## Visual system and shell

- Chalk mark, header proportions, Geist/Geist Mono typography, monochrome palette, blue selection, semantic route colors, hairlines, control radii, compact density, hover/focus treatments, and coaching voice (a three-colour Unit palette on chips, dots and printed badges, one focus ring for every focusable element, a selection rule on the current call, and one disabled treatment are the ADR 0051 exception; the pill's dot takes the Play's Unit).
- Editor, Demo/Tour, Present, and Print views with their existing hierarchy and transitions (the header now names Editor · Playbooks · Game Day and reaches Demo through Help, Present through a button, and Print through Print & export — the ADR 0044 exception).
- Present's bar carries visible Previous/Next variation controls and a labeled Back beside its esc hint (ADR 0046, additive); the Game Day reader is a new destination over prepared revisions.
- The Print view is the Print & export workflow — source, format, preview, print or save — with the original's sheet as its default preview (the ADR 0047 exception; the Print ratchet was raised under it).
- Editable Play title, category treatment (now document-bound Unit · Type — ADR 0041), undo/redo, More, Export (now Print & export), and Save/version menus.
- Full-window field-first workspace with independently collapsible tool rail and inspector, Focus mode, persistent chrome state, status bar, and contextual help (the inspector's idle order and folding are the ADR 0043 exception; Clear moving from the rail to More → Clear…, the optional tool names, and Block reading Blitz on a defender are the ADR 0044 exception; the rail keeping only Text and Trash, lines starting from the selected man, eleven a side as the roster, and nobody crossing the ball are the ADR 0052 exception).
- Canvas camera behavior, zoom cluster, fit, fit-to-selection, persistent per-Play camera, minimap, and page/field presentation presets.
- Tablet and narrow screens: docked inspector, drawer, or reading shell by width, real 44 px touch boxes, a reflowing library grid, and search focused on request (ADR 0045, additive — nothing changes at the desktop viewport).

## Drawing and direct manipulation

- Player placement and editing, all existing symbols, fill modes, labels, sublabels, colors, sides, groups, and role behavior.
- Route creation and node editing; straight and curved segments; control handles; inserted nodes; segment selection; branches/choice routes; alternate routes; and route-to-player attachment.
- Route kinds and semantics including route, motion, block, zone, blitz, and stunt with their existing defaults.
- Line and segment styles, endings, block ticks, colors, read numbers, assignments, conversions, and coaching notes.
- Free labels, boxes, outlines, leaders, colors, collision handling, and annotation-layer visibility.
- Select, additive select, marquee, move, pan, zoom, pinch, two-finger pan, mirror, strength flip, copy/paste, delete, clear-by-layer, clear-side, and clear-all behavior.
- Yard-aware angle snapping, depth behavior, ball/hash placement, field landmarks, snap guides, and the snapping toggle.
- Right-click and long-press actions, command palette, shortcuts, guided hints, and status feedback.

## Football intelligence

- Built-in offensive Formation library with search, personnel/family grouping, favorites, custom Formations, structured roles, hash/ball metadata, and save-Formation behavior.
- Hover and pinned Formation previews, match hairlines, Apply/Cancel, all apply modes, role-aware realignment, route/label preservation, and one-step undo.
- Formation recognition, status naming, semantic counterpart strength flip, and geometric fallback.
- Built-in defensive fronts, coverages, and pressures with favorites, search, insertion, zone drops, blitz paths, and preservation of the opposite unit where intended.
- Ball hash selection, line calls, quick route presets, quick block presets, defensive presets, Concepts, route rules, and jet/motion rules.
- Flexible player counts for offense, defense, special teams, and drill cards.

## Play library and Concept families

- Save/update, new Play, guarded transitions, delete/confirm, rename, duplicate/variation, and current-Play identity.
- Concept-to-variation tree, persisted disclosure state, current marker, distinguishing variation names, and seeded examples.
- `This play`, `Whole concept`, and `Pick…` edit scopes with role-based propagation, visible scope markers, skip reporting, and undoable library changes.
- Detach-from-Concept behavior, Concept notes/tags, Formation-derived variation naming, and explicit alignment push.
- The current library panel remains available even if a season-scale Playbook browser is added.

## Workspace, teaching, and animation

- Coach, Player, and Print type presets; Reads, Assignments, Notes, and Text layers; minimum readable text; and label collision handling.
- Present mode with full-window field, hidden authoring chrome, variation stepping, and return-to-editor behavior.
- Print preview using the same renderer and page presets as output.
- Guided Demo/Tour sequences, examples, inspector teaching panels, playback, and stepping.
- Route delay, speed, and hold; pre-snap motion; constant-distance straight/curve movement; endpoint holds; defender movement; and ghost trails.
- Play, pause, reset, 0.5x/1x/2x speed, scrubbing, time/snap state, keyboard playback, and editability at frozen frames.
- Timing survives save, mirror, variation, Formation realignment, and restore.

## Exports and coaching outputs

- PNG, SVG, and Print the field.
- Practice cards, call sheet, install page, position view, quiz and answer key, presentation slide, wristband, scout card, progression strip, and full Playbook.
- Existing pickers, group fading, tag/category grouping, page sizes, typography, cut lines, assignment/progression content, layer settings, and print semantics.
- Frame-sequence animation export and manifest.
- Coverage zones, routes, branches, labels, player styling, and field geometry remain consistent across every renderer and document.

## Durability, recovery, and input

- Working-document persistence, explicit Play save/update, named snapshots, per-Play history, human labels, restore, recovery notice, and undoable restore.
- Undo/redo for document and scoped library changes, truthful dirty state, guarded transitions, and visible failure reporting.
- Mouse, keyboard, touch, trackpad, and stylus behavior including pinch, two-finger pan, long-press, precision tolerance, palm rejection, and coarse-pointer targets.
- Existing preferences, favorites, camera, library disclosure, layers, chrome, Formations, seeds, and Plays migrate without loss.

## Additive production extensions

IndexedDB repositories and migrations, persistent hash-guarded undo, encrypted backups, Clerk access, Convex replication, Conflict Inbox, private R2 images, external Film References, immutable Share Publications, scale search/virtualization, PWA installation, observability, backups, and isolated environments extend the original rather than replace it.

| Extension                                        | What it adds                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | What it must not change                                                                                                                                                                                                                    | Decision              |
| ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------- |
| Empty-defense field CTA                          | On Editor offense-only Plays, a low-chroma **Add a defense** control on the empty defensive half opens the existing Defenses overlay (same path as **No defense yet** / `⇧⌘D`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Defenses catalogue, apply semantics, freehand Letter/`Z` path, auto-apply of any default call, Demo/Present/Print chrome                                                                                                                   | ADR 0040 / ARC-182    |
| Document-bound Unit · Type pill                  | The header's category `<select>` (seeded **Pass**, bound to nothing) becomes a pill that reads the open Play as **Unit · Type**, offers only the Unit's live Types plus **Unclassified**, writes back through `set-unit` / `set-play-type`, names what a Unit change drops before applying it, and lets the Coach define Types in the Playbook's existing model. Cards, chips, call sheets and printed pages read the same words                                                                                                                                                                                                                                                                                                                                      | Pill geometry, hairline, dot, type and position; play name, undo/redo and menus; the diagram on any classification change; legacy Defense/Special plays are never guessed into a Type                                                      | ADR 0041 / GitHub #63 |
| Game Plans and prepared revisions                | A Playbooks workspace (More menu, command palette, Playbook browser) holds named Game Plans per coordinator unit: sections, calls referencing Plays by id, unique call codes, bulk additions with a stated count, saved searches expanded only on request, and **Prepare for game**, which freezes an immutable revision that call sheets, wristbands, handouts and the game-day reader consume. Library edits flag a plan as behind its revision and never rewrite a packet                                                                                                                                                                                                                                                                                          | The library panel, Playbook browser cards, Export menu entries and the library-wide outputs they already produce; Play documents are never copied or edited by a plan                                                                      | ADR 0042 / GitHub #66 |
| Contextual inspector with progressive disclosure | The idle inspector leads with Play setup (Formation, Ball on, the concept and line call on the field) or, on a defensive play, the Defensive call; Opponent look, Library, Field, Playbook settings, History and Print & export fold with their heading and a one-line summary in view, remembered per device. Line calls and concepts become one searchable catalogue with Favorites and Recent. Layers move to a popover in a thin inspector bar that also carries the collapse control; ⌥1/⌥2 work. Selected player and route panels lead with assignment and coaching; appearance and timing fold. Sidebar Save removed; library delete × on hover/focus                                                                                                          | Every control, its words and its command; Formation and Defense browsers; the library panel's contents and scopes; option routes, alternates, blocking, motion, coverage, timing and propagation; the Play document                        | ADR 0043 / GitHub #64 |
| Selection-first assignments                      | The left rail keeps Text and Trash (plus Done on a phone and the collapse control); Select is the field's standing mode. A line by hand starts from the selected man — a Draw row in his inspector (Route, Motion, Block for a receiver or back; Block for a lineman; Zone drop and Blitz for a defender), the R M B Z keys, the blue dot, and the palette's four Draw commands. There is no Player tool: men arrive with a Formation or a defensive call, and deleting a selected man clears his lines rather than removing him. A drag, nudge or paste holds a man a yard on his side of the line of scrimmage, a dragged group with him. The Aa names toggle and the snap button leave the rail; S and Shift still switch snap                                     | Quick calls, alternates, flip, the line inspector, the blue-dot drag, Text and its key, Trash and ⌫, More → Clear…, the guided tours as filmed                                                                                             | ADR 0052              |
| Header destinations and actions                  | The tab strip reads Editor · Playbooks · Game Day; Playbooks holds the Playbook browser and Game plans as pages, Game Day the prepared plan. New play and Present are labeled header buttons; Export becomes Print & export leading with Print preview; Help holds Demo, the five tutorials, Keyboard shortcuts and Command palette. Clear leaves the rail for a Clear… page of More with its scopes and undo named; an Aa control shows tool names beside the glyphs, a tap away and remembered per device; Block reads Blitz on a defender and draws a blitz path                                                                                                                                                                                                   | Every command, shortcut and menu label beneath the renamed buttons; the seven tools, their keys, glyphs and the blue-dot gesture; Snap; the Demo, Present and Print views once open; the export groups' items and order                    | ADR 0044 / GitHub #65 |
| Adaptive tablet layout and real touch targets    | The shell drops its 1024 px floor: the inspector docks at 1180 px and 1024 px as before and becomes a drawer over the field below that; the header gives up its brand, New play and Help below 1024 px (More and the palette keep them); below the editor's floor the reading shell carries the three destinations and an explicit **Edit on this screen**. Coarse-pointer targets are real boxes — primary actions 44 × 44, compact controls 32 px with spacing — instead of a pseudo-element expansion. The Playbook browser lays out one to six columns by measured width with the virtual rows grouped from the same number, and search focuses on request rather than on entry                                                                                   | Nothing at the desktop parity viewport with a fine pointer: no rule below 1024 px or under `(pointer: coarse)` applies there, and the measured grid is the original's four columns at the browser's width; every control, word and command | ADR 0045 / GitHub #68 |
| Game Day reader                                  | The Game Day destination opens a prepared revision (never the live library) with unit, plan, opponent, prepared stamp, situation tabs, favorites, code/name search, list or grid, Previous/Next and Back to section; readiness is checked against this device's plays and images; notes, called counts and results live beside the revision on the device; a newer prepared revision is offered, never swapped in. Present gains touch Previous/Next and a labeled Back                                                                                                                                                                                                                                                                                               | Present's stage, type scale and keyboard; the editor; the Play document; plans and revisions themselves                                                                                                                                    | ADR 0046 / GitHub #67 |
| Print & export workflow                          | The Print tab becomes a four-step workspace: source (current play, selected plays in the Coach's order, a game plan's packet or current plays, or explicitly the full playbook), format (the fifteen outputs in four groups, each naming its paper), preview at page width with margins, page breaks and a sheet count, then Print… from a hidden frame, Save as PDF… labeled as the browser's choice, Open in a new tab with a blocked pop-up reported, or Download. Detail and monochrome are explicit; Recent presets sit in the menu                                                                                                                                                                                                                              | The thirteen menu entries, their labels, order and groups, and the generators behind them; the field sheet and its caption; the palette row; prepared revisions' fixed presentation                                                        | ADR 0047 / GitHub #69 |
| Configurable coordinator call sheets             | A game plan's call sheet is laid out per plan: OC, DC or special-teams template, the sections that print in the Coach's order with a title, accent and side, columns read off the Play or ruled blank, density, one or two sides, small diagrams, and the notes column; every call prints its stable code, long names wrap, accents carry a letter and a rule for grayscale, the header names plan, opponent, unit and prepared version, and what would flow or is missing is said before printing                                                                                                                                                                                                                                                                    | The original tag-grouped Call sheet for a selection or the library; the plan's sections and codes; the generators                                                                                                                          | ADR 0048 / GitHub #70 |
| Configurable wristband inserts                   | A game plan's wristband is cut to the Coach's cell width and height, cells across and down, with size presets named by their dimensions; cells show code + name, code + diagram, or both; the band starts from the plan's own calls to confirm, ordered by drag or arrows with an optional short name; inserts paginate without omission; cut guides, corner ticks, a one-inch calibration bar and a print-at-100 % note print on every sheet; duplicate codes, missing plays and names that will not fit are said first                                                                                                                                                                                                                                              | The original eight-cell band and picker for a selection or the library; the plan's codes; the generators                                                                                                                                   | ADR 0049 / GitHub #71 |
| Binder and handout layouts                       | A binder from an explicit source: one play a page, Letter or A4, a punch-side gutter mirrored on facing pages for duplex, section dividers, contents, page numbers and a notes area as choices, codes beside names and the prepared revision on the cover — with page numbers read off the preview's own layout, contents included, so a long contents or a dense table moves what follows by what it took. A handout one, two or four to a sheet with compact or full assignments and notes; cards keep their size and say what they cannot show                                                                                                                                                                                                                     | The original Full playbook entry and output; scout and practice card presets; the generators                                                                                                                                               | ADR 0050 / GitHub #72 |
| Semantic colour and state contrast               | A three-colour Unit palette (Offense azure, Defense wine, Special teams ochre), checked for contrast, colour-vision simulation and distance from the field's route and coverage colours, on one labeled badge — Playbook cards, game plan rows and headers, the Game Day list and reader title, printed call sheets, game-plan sheets, teaching pages and binder covers — and as the dot on Unit chips, Unit segments and the header pill. One focus ring on every focusable element; a selection rule on the current call and the active format; one disabled treatment. Install and offline-ready notices condense to a quiet status-bar word once acknowledged on the device; a failed offline preparation stays an alert; the shell never claims a plan is cached | Geist, the neutral canvas, hairlines, radii and density; the pill's geometry and words; the route and coverage colours and saved route colours; the printed field sheet; every command, label and shortcut; the field's blue selection     | ADR 0051 / GitHub #73 |

## Evidence required before parity can be claimed

- Screenshot baselines for every top-level mode, panel, menu, modal, selection state, and responsive state.
- A control/action inventory generated from source and manually verified in the running prototype.
- Complete shortcut and pointer/touch/stylus gesture maps.
- Serialized fixtures for every player, route, branch, label, Formation, defense, Concept family, timing, and output state.
- Golden samples for every export and printed document.
- Behavior scripts for all feature groups, including corrected expectations for documented prototype defects.
